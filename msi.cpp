#include <iostream>
#include <vector>

using namespace std;

int msi(vector <int> x, int i, vector <int> memo){
    if(i == x.size()) return 0;
    if(memo[i] != -1) return memo[i];
    if(x[i]>x[])
}